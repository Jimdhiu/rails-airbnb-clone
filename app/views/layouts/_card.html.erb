
<div class="container">
  <div class="row">

    <div class="col-xs-12 col-sm-4">
      <div class="card-link"><%=link_to boat_path(@boats[-1]) do%>
        <% if @boats[-1].photo? %>
          <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path @boats[-1].photo.path %>')">
        <% else %>
            <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= image_path "no_boat_photo.png" %>')">
        <% end %>

      <% end %>


              <div class="card-price"><%= @boats[-1].price %> €</div>
              <div class="card-address"><%= @boats[-1].address %></div>
              <div class="card-description">
                <h2><%= @boats[-1].name %></h2>
                <p>Evaluation ***</p>
              </div>
            </div>
          </div>
      </div>
    </div>


  </div>

