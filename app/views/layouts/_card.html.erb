
  <div class="container">
    <div class="row">
    <%@boats.each do |boat| %>
      <div class="col-xs-12 col-sm-4">
        <div class="card-link"> <%=link_to boat_path(boat) do%>
          <% if boat.photo? %>
            <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= cl_image_path boat.photo.path %>')">
          <% else %>
              <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url('<%= image_path "no_boat_photo.png" %>')">
          <% end %>

                <div class="card-price"><%= boat.price %> €</div>
                <div class="card-address"><%= boat.address %></div>
                <div class="card-description">
                  <h2><%= boat.name %></h2>
                  <p>Evaluation ***</p>
                </div>
              </div>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
  </div>







