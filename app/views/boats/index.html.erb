<h1>Votre recherche</h1>
<% if @message != "" %>
  <p><%= @message %></p>
<% end %>
<div class="container">
  <div class="row">

      <% @boats.each do |boat| %>
      <%= link_to boat_path(boat) do %>
          <div class="card-link"><%=link_to boat_path(@boats[-1]) do%>
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.2)), url("#{<% @boats[-1].photo%>}");">
          <div class="card-price"><%= @boats[-1].price %> €</div>
          <div class="card-address"><%= @boats[-1].address %></div>
          <div class="card-description">
            <h2><%= @boats[-1].name %></h2>
            <p>Evaluation ***</p>
          </div>
        </div>
      <%end %>
      <% end %>
    <% end %>
  </div>
</div>
